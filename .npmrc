# Fail install if Node/pnpm engines in package.json don't match.
engine-strict=true

# NOTE: pnpm-native settings (minimum-release-age, auto-install-peers) moved to
# pnpm-workspace.yaml — pnpm 11 no longer reads them from .npmrc. See that file.
